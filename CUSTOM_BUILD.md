# 短信转发（Fossify Messages + 多渠道 SMS Gateway）

这是基于 Fossify Messages 深度定制开发的现代化短信客户端与多渠道转发网关。它既作为 Android 默认短信应用接收 `SMS_DELIVER`，也兼容部分系统使用的 `SMS_RECEIVED` 广播，支持收件箱、发件箱、通讯录或手动号码、独立批量发送，支持「经典日常模式」与「现代化 SMS Gateway 开发版工作台」双模式无缝切换，并把新短信通过 11 大主流第三方渠道即时推送。

## 1.1.2（versionCode 1102）

- 架构全面升级为 Jetpack Compose 5 大核心工作台：【信息中心】、【大盘监控】、【规则工坊】、【通道中枢】、【运维诊断】。
- 经典版与开发版全态数据双向互通，统一 15 大通道底层加密 Token 仓库与开关状态。
- 消息模板全面升级与两版共享：经典版「设置 → 功能」加入「消息模板」独立入口，支持 5 大预设模式（紧凑/标准/详细/Emoji/自定义）、12 大变量一键插值与智能提取验证码实时预览沙箱。
- 11 大主流推送通道全量支持：微信测试号、Telegram Bot、通用自定义 Webhook、Discord、企业微信、钉钉、飞书、Bark、PushPlus、邮件 SMTP、Gotify。
- 视觉与交互体验重构：全新悬浮式毛玻璃胶囊 Dock 导航、通道卡片防挤压重构、会话时间戳与卡槽智能对齐、顶栏深色沉浸融合。
- 底层稳定性加固：彻底重构全工程 EventBus 订阅结构，100% 杜绝 Android 10/11 因反射 `PictureInPictureUiState` 引发的启动闪退；Outbox 离线事务队列断网暂存与自愈恢复。
- 正式 Release 已彻底移除 Fossify 假版本弹窗链路，并在流水线中扫描阻断回归。

## 使用方式

1. 安装 APK，首次打开时将“短信转发器”设为默认短信应用并授予短信、电话、联系人和通知权限。
2. 主界面可随时在右上角菜单中在「经典日常模式」与「开发版 SMS Gateway 工作台」之间自由切换。
3. 进入「设置 → 转发通道」（或开发版【通道中枢】）。
4. 选择要启用的推送渠道（如微信测试号、Telegram、Webhook、PushPlus、钉钉等），填入对应 Token / Webhook URL 并保存。
5. 点击卡片右侧的「测试」按钮，在线验证推送是否成功（测试记录会自动归档至大盘）。
6. 使用另一台手机发送一条普通短信进行完整端到端测试。

## 批量发送

1. 点击主界面右下角绿色“+”，选择“批量发送短信”。
2. 搜索、逐个勾选或全选当前搜索结果；也可以直接输入 `10086`、手机号或带国家区号的号码并点击“添加号码”。
3. 选择发送 SIM 卡并填写正文。
4. App 会实时显示收件人数和预计计费短信条数，确认后逐个独立发送。

每位收件人收到的是独立 SMS，彼此不会看到号码。为降低误发、系统限流和运营商风控风险，单次限制 30 个号码，任务间隔可在设置中选择立即或 1–5 秒。

## 转发行为与模板变量

- 支持在自定义模板中使用 12 大动态变量：
  - `{{FROM}}` 发信号码、`{{SMS}}` 短信正文、`{{RECEIVE_TIME}}` 接收时间、`{{CONTACT_NAME}}` 联系人姓名
  - `{{SIM_SLOT}}` 卡槽标识、`{{RECEIVER_NUMBER}}` 接收卡号
  - `{{DEVICE_NAME}}` 设备型号、`{{BATTERY_INFO}}` 电池电量状态、`{{IP_LIST}}` 当前 IP 地址、`{{NET_TYPE}}` 网络类型、`{{APP_VERSION}}` 应用版本、`{{CURRENT_TIME}}` 当前时间
- 所有通道密钥（AppSecret、Bot Token、Headers 等）均使用 Android Keystore 的 AES/GCM 密钥安全加密保存在本机。
- 转发任务要求网络可用；网络中断或休眠时 Outbox 离线事务队列自动暂存，网络恢复后使用指数退避自动重试。
- 以短信接收时间、发送方和正文生成唯一指纹，彻底避免重复推送。

## 华为、小米与各品牌后台设置

在“设置 → 应用和服务 → 默认应用 → 短信”中确认本应用为默认短信应用。再到“应用启动管理”关闭自动管理，允许自启动、关联启动和后台活动。建议关闭省电模式（电池优化设为不限制）并允许休眠时保持网络连接。

如果历史短信显示不完整，可在主界面右上角菜单选择“重新同步全部短信”。应用会合并系统短信库和本地缓存，并重建缺失的会话列表。

## 构建

需要 JDK 17、Android SDK 36 和可访问 Google Maven/Maven Central 的网络环境。

```bash
# 编译 Debug APK
./gradlew :app:assembleCoreDebug

# 编译 Release 正式签名 APK
./gradlew :app:assembleCoreRelease
