# 定制版待统一处理清单

当前阶段：**问题收集中，暂不修改功能代码、不重新构建 APK。**

后续新增问题继续按编号追加；收到“统一修改”的明确指令后，再集中修改、回归测试、统一签名并构建新版本。

## 待处理问题

### ISSUE-001：首次启动误报“Fake version”

- 优先级：P0
- 状态：待处理
- 复现：安装 build 1005，首次打开并开始同步短信时弹出英文警告。
- 表现：提示当前应用是假版本，并要求从 Fossify 官网下载。
- 原因定位：`MainActivity` 调用了 Fossify Commons 的 `checkAppSideloading()`；定制版包名及签名不同于 Fossify 官方版本，因此被误判。
- 计划修改：移除定制版中的侧载/官方签名检测，不影响 Android 系统自身的安装安全提示。
- 验收标准：全新安装、清除应用数据后首次启动均不再出现该弹窗。
- 截图：`docs/issue-evidence/issue-001-fake-version-dialog.jpg`

### ISSUE-002：首次同步时加载文字过淡

- 优先级：P1
- 状态：待处理
- 表现：“正在加载信息…”接近透明，在白色背景上难以辨认；空列表和搜索提示也可能存在同类问题。
- 原因定位：占位文字仍使用旧布局中的透明度设置，并可能受到 Fossify 动态文字颜色影响。
- 计划修改：主界面所有占位、加载、空状态文字统一使用不透明深灰色；仅通过颜色区分层级，不使用整体 Alpha。
- 验收标准：加载、空状态和无搜索结果文字在白底上清晰可读，Alpha 固定为 1。
- 截图：`docs/issue-evidence/issue-002-loading-text-opacity.jpg`

### ISSUE-003：APK 体积约 31 MB

- 优先级：P2
- 状态：待统一发布配置时处理
- 原因定位：当前 GitHub Actions 构建的是未压缩、未裁剪的 `coreDebug` 包；DEX 压缩后约占 25 MB，资源表约占 5.5 MB。
- 计划修改：正式构建启用 R8、资源裁剪和必要的保留规则；清理确认未使用的依赖和资源。
- 验收标准：转发、短信收发、通讯录、批量发送等回归通过的前提下明显减小安装包。

### ISSUE-004：建立永久统一的应用签名

- 优先级：P1
- 状态：待发布配置
- 当前情况：build 1005 是临时 Debug 包，包名带 `.debug`，不适合作为长期升级渠道。
- 计划修改：生成并离线备份唯一 Release Key；密钥文件和密码不提交到仓库，通过 GitHub Secrets 注入构建；以后统一使用同一签名和正式包名。
- 验收标准：后续正式版本可以直接覆盖升级，签名校验一致，版本号正常递增。
- 迁移提醒：已经安装的 Debug 版不能直接覆盖为正式 Release 版，需要首次迁移并重新设置默认短信应用。

### ISSUE-005：关于页缺少定制版仓库地址

- 优先级：P1
- 状态：待处理
- 表现：关于页保留了常见问题、已知问题、第三方许可和版本号，但没有显示本定制版的代码仓库。
- 目标仓库：`https://github.com/2756826865/sms-forwarder-huawei`
- 计划修改：在关于页增加“项目仓库”条目，显示仓库名称或地址，点击后交给系统浏览器打开。
- 保持删除：不恢复 Fossify 捐赠、评价应用、分享、贡献者、Reddit、Telegram、更多应用等无关入口。
- 验收标准：关于页能够清晰看到并正常打开上述仓库地址，版本号和第三方许可仍然保留。

### ISSUE-006：SIM1 / SIM2 转发名称不能自定义

- 优先级：P1
- 状态：待处理
- 表现：转发消息中只能显示系统生成的 `SIM1_运营商`、`SIM2_运营商`，无法按实际用途区分两张卡。
- 计划修改：在“设置 → 短信转发”中增加“双卡名称”设置，分别提供 SIM1、SIM2 自定义名称输入框。
- 示例：SIM1 可命名为“工作卡”，SIM2 可命名为“私人卡”或“验证码卡”。
- 生效范围：PushPlus、钉钉、飞书、企业微信应用消息、电子邮箱全部使用同一套自定义名称。
- 回退规则：自定义名称为空时继续显示系统 SIM 槽位及运营商名称；单卡手机只显示当前有效卡。
- 绑定规则：名称按物理卡槽保存，不按临时 Subscription ID 保存，避免重启或运营商刷新后对应关系错乱。
- 验收标准：分别从 SIM1、SIM2 收到短信时，所有启用渠道都能准确显示对应的自定义卡名。

### ISSUE-007：转发消息模板信息重复、层级不清晰

- 优先级：P1
- 状态：待处理
- 表现：电话号码同时出现在标题和“发送号码”字段中；短信正文夹在号码、SIM、时间之间；`SIM2_中国移动`偏系统化，不便快速阅读。
- 默认方案：使用“内容优先”的简洁模板。
- 推荐标题：`【自定义卡名】联系人名或发送号码`
- 推荐正文：第一行直接显示短信内容；标题已经显示号码时不再重复“发送号码”；末尾按需显示接收时间。
- 联系人规则：能读取联系人时优先显示联系人名，完整号码放到正文次要位置；未识别时显示原始号码。
- 建议设置：提供“简洁 / 标准 / 详细”三种模板，并在设置页显示实时预览；所有转发渠道共用模板规则。
- PushPlus 限制：详情页底部广告属于 PushPlus 网页，应用端无法删除；可提示用户启用 PushPlus 激活消息以直接显示文本，或使用其他转发渠道。
- 验收标准：默认模板无重复字段，正文一眼可见，SIM 自定义名称与 ISSUE-006 保持一致。
- 截图：`docs/issue-evidence/issue-007-forwarding-template.jpg`

### ISSUE-008：删除整条会话时绕过“最近删除”

- 优先级：P0
- 状态：已定位，可修复，待统一修改
- 表现：在短信列表删除整条会话后，短信没有进入“最近删除”，而是直接永久删除；“最近删除”页面因此看不到该会话。
- 原因定位：会话列表的删除入口直接调用 `deleteConversation(threadId)`，该方法会同时删除 Android 短信数据库和应用本地数据库中的消息；它没有判断 `config.useRecycleBin`，也没有写入本地 `recycle_bin` 表。会话详情页的“删除整条会话”入口也存在同样问题。
- 已有基础：单条短信删除已经支持 `moveMessageToRecycleBin(messageId)`，最近删除页面、恢复操作和回收站数据表均已存在，因此无需删除该功能入口。
- 计划修改：
  1. 新增“整条会话移入最近删除”方法，逐条写入回收站标记，不删除系统短信数据库及本地消息正文。
  2. 会话列表删除和会话详情页整条删除都根据“使用最近删除”设置选择移入回收站或永久删除。
  3. 主列表查询排除所有消息均已进入回收站的会话，避免留下空会话；恢复后原会话重新出现。
  4. 从“最近删除”执行彻底删除或到期清理时，才删除 Android 短信数据库和本地数据库中的对应消息。
  5. 保持定时短信等特殊记录不被错误移入回收站，并处理旧会话恢复后再次收到新短信的情况。
- 验收标准：
  1. 删除单条短信后，可在最近删除中看到并恢复。
  2. 从主列表删除整条会话后，主列表立即隐藏该会话，最近删除中可看到该会话的全部短信。
  3. 恢复整条会话后，会话及短信原样回到主列表。
  4. 在最近删除中彻底删除后，系统短信数据库和应用本地数据库均不再保留对应短信。
  5. 自动到期清理、双卡短信、同一号码再次收信均不产生空会话、重复消息或错乱。
- 产品决定：保留设置中的“最近删除”，不因当前删除链路缺陷移除入口。

### ISSUE-009：增加小米 MIUI / HyperOS 兼容与引导

- 优先级：P1
- 状态：具备通用兼容基础，待真机适配与回归
- 当前结论：应用并非只支持华为。短信收发、默认短信应用、双卡选择和短信广播均使用 Android 标准 `Telephony`、`RoleManager.ROLE_SMS`、`SubscriptionManager` 接口，代码中没有写死华为品牌判断；当前只能称为“华为 EMUI / HarmonyOS 4.x 已实机验证”。
- 系统范围：项目最低支持 Android 8.0（API 26），目标 SDK 36；小米 MIUI 及仍兼容 Android APK 的 HyperOS 原则上可共用同一正式 APK、包名和签名。
- 注意：资源中 `miui_*` 仅是当前仿小米短信 UI 的样式命名，不代表已经完成小米系统级兼容测试。
- 计划修改：
  1. 首次启动检测小米/Redmi/POCO 设备，增加针对 MIUI / HyperOS 的权限检查页，但不把业务代码分叉成“小米版”。
  2. 引导用户将本应用设为默认短信应用，并检查短信、电话状态、联系人、通知等必要权限。
  3. 增加“小米后台运行检查”：自启动、后台运行/省电策略设为无限制，并提供跳转系统设置；跳转失败时回退到标准应用详情页。
  4. 对收到短信后立即转发、锁屏转发、清理后台后转发、重启后转发进行专项测试。
  5. 真机验证 SIM1/SIM2 的 `subscriptionId`、默认发信卡、收信卡识别及自定义卡名映射，兼容单卡和双卡。
  6. 保留标准 Android 实现作为主路径，厂商设置入口只作为辅助引导，避免系统升级后完全失效。
- 验收机型建议：至少一台较新的小米/Redmi HyperOS 双卡设备；如需兼容旧机，再补一台 MIUI 14 设备。
- 验收标准：默认短信角色设置成功；前台、后台、锁屏、重启后均能及时收信并触发转发；双卡收发与标识正确；通知、批量发送、回收站和短信同步正常。

### ISSUE-010：主界面“＋”按钮改为圆形并统一绿色

- 优先级：P1
- 状态：待统一修改
- 目标入口：主界面右下角的新建短信按钮；点击行为继续保持“立即新建短信”，不展开功能菜单。
- 表现：当前按钮呈圆角方形，绿色也与参考界面不一致。
- 计划修改：强制使用正圆形 FAB，避免受到应用全局圆角或 Fossify 自定义外观影响；图标使用居中的白色细线加号。
- 目标色值：参考图主体色采样为 RGB `29, 206, 56`，即 `#1DCE38`；替换当前 `#19C95A`。
- 使用范围：该绿色优先作为主界面新建短信 FAB 的固定操作色；其他页面的提交/确认按钮是否同步使用，统一修改时按界面层级检查，避免所有控件无差别变绿。
- 验收标准：按钮宽高一致、轮廓为完整正圆、白色加号视觉居中；浅色背景下颜色与参考图一致，按压、禁用和深色模式下不变成圆角方形。

### ISSUE-011：设置页增加“定时发送”管理入口

- 优先级：P1
- 状态：已有底层能力，待统一整理入口和界面
- 当前基础：项目已具备单条短信选择日期时间、预约发送、编辑、取消、开机后恢复计划任务和精确闹钟权限处理，继续复用现有实现，不重复开发第二套调度逻辑。
- 设置位置：`设置 → 定时发送`，作为独立功能项；它与“批量发送短信设置”中的每个号码间隔 `立即 / 1–5 秒`互不影响。
- 管理页内容：按发送时间排列所有待发送短信，显示收件人/联系人、短信摘要、计划时间、SIM 卡及当前状态。
- 支持操作：新建定时短信、修改收件人/内容/时间/SIM、立即发送、取消任务；取消前需要确认，避免误操作。
- 新建入口：管理页右下角提供圆形绿色“＋”；会话页附件菜单中的“定时发送”入口继续保留，两处最终进入同一套编辑流程。
- 系统状态：页面显示精确闹钟权限和任务是否有效；权限不足时提供明确提示及系统设置入口，不能显示为已预约成功。
- 异常处理：关机重启、应用升级、修改系统时间/时区后重新校准；发送失败保留记录并显示失败原因，允许重试，不能静默丢失。
- 验收标准：单卡/双卡均能按指定时间发送；重启后任务仍有效；新建、编辑、立即发送和取消后列表状态即时更新；同一任务不会重复发送。

## 本轮不执行

- 不删除 `checkAppSideloading()`。
- 不调整文字透明度。
- 不改签名或构建类型。
- 不修改关于页。
- 不增加 SIM 自定义名称设置。
- 不调整转发消息模板。
- 不修改删除链路，也不移除“最近删除”。
- 不在本轮加入小米厂商适配代码。
- 不在本轮修改主界面“＋”按钮形状和颜色。
- 不在本轮新增“定时发送”管理页。
- 不生成 build 1006。
