# SMS Forwarder 1.0.6

公开发布版本。基于 Fossify Messages 二次开发，可设置为 Android 默认短信应用，面向 Android 8.0–16 构建。

主要适配华为 EMUI/HarmonyOS、荣耀 MagicOS、小米 MIUI/HyperOS、vivo/iQOO，同时兼容 OPPO、一加、realme、三星和 Pixel 等主流 Android 设备。

---

## 本次更新（相对 1.0.5）

### 新增功能

**转发规则（默认关闭）**

- 新增「转发 → 转发规则」。
- 支持关键词、正则、来源 SIM 卡过滤，并指定规则作用的转发渠道。
- 支持三种作用范围：仅扩展渠道、含短信直发、含远程短信指令。
- 关键词支持逗号、竖线 `|`、分号或换行分隔。
- 提供填写示例与规则测试。

**远程转发功能（默认关闭）**

- 新增「转发 → 远程转发功能」入口，整合以下两项：
  - **短信远程指令**：白名单授权；命令 `/短信发送 [SIM1|SIM2|默认|系统默认] 手机号 内容`；1 小时 5 次 / 10 分钟去重；命令短信不进入正常转发。
  - **钉钉 Stream 远程指令**：Client ID / Client Secret 长连接；群聊 @机器人 或私聊发令；可指定 SIM1 / SIM2 / 默认。
- **发送回执**：本地执行日志；可选渠道回执（提交 / 成功 / 失败 / 送达，与正常转发独立）。
- 远程发送支持 SIM 解析（SimSendResolver），日志显示实际所用 SIM。

**新增转发渠道**

- **Bark**：服务地址、Device Key、允许 HTTP、发送测试。
- **Gotify**：服务地址、Application Token、允许 HTTP、发送测试。

**转发模板扩展**

- 新增 **Emoji** 模板。
- 新增 **自定义** 模板，占位符：`{sender}` `{name}` `{body}` `{time}` `{sim}`。

---

### 功能改进

**批量发送**

- 合并为同一页：收件人 + 正文 + 发送 SIM + 发送间隔（立即 / 1–5 秒）。
- 入口：**设置 → 批量发送**，或首页 **+ → 批量发送**。
- **发送设置**可折叠，默认收起；摘要显示当前 SIM 与间隔；置于联系人列表上方。
- 移除设置页独立项「批量发送短信延迟」；**设置 → 定时发送** 仍独立，与批量间隔互不影响。

**设置页重组**

- **功能**：短信转发、定时发送、批量发送、骚扰拦截、最近删除、显示首页底部导航。
- **系统**：短信同步管理、后台运行与系统兼容。
- **其他**：关于。

**首页导航**

- 底部「短信 · 转发 · 设置」胶囊导航默认开启。
- **设置 → 功能 → 显示首页底部导航** 可关闭；关闭后 **+** 位置不变，返回首页立即生效。

**后台运行与系统兼容**

- 继续完善关键状态检测与品牌专属建议（华为 / 小米 / vivo 等）。
- 优化默认短信路由、WRITE_SMS、通知与电池策略相关提示。

**短信转发**

- 转发页新增转发规则、远程转发功能入口及状态摘要。
- 各渠道设置页（含 Bark、Gotify、远程指令、钉钉远程、转发规则）统一 MIUI 风格设置页。

---

### 界面调整

- 批量发送页标题移至顶部导航栏居中。
- 新建短信、会话列表、转发设置、兼容性体检等页面视觉与间距继续统一。
- 邮箱、批量发送等页面 Spinner / 输入框修复白底白字不可读问题。
- 设置、转发、关于等页面统一浅灰色返回箭头样式。

---

### 修复

- 修复转发模板「自定义」模式下正文重复（原文 + 模板各一行）。
- 修复「自定义」模板：取消编辑仍切换模式、已选自定义时难以再次编辑的问题。
- 修复 `MultiChannelForwardWorker` 编译问题（`withContext` 大括号缺失）及部分中文乱码。
- 修复首页底部导航显示、对比度与 FAB 位置相关问题。
- 修复部分设备兼容性检测页状态展示与可读性问题。

---

### 构建与工程

- 版本号 **1.0.6**，versionCode **1013**。
- CI：优先官方 Maven 仓库、稳定 KSP 插件解析、恢复 Gradle Wrapper 可执行权限。
- 依赖与 Gradle 配置小幅更新。

---

### 文档

- 重写「关于 → 常见问题」（18 条）、「已知问题」（15 条）。
- 更新 `README.md`：转发规则、远程转发、批量发送、设置分组、回归清单。
- 远程指令 / 转发规则 / 钉钉远程设置页补充命令示例与填写说明。

---

## 转发功能（1.0.6 完整列表）

| 渠道 | 说明 |
|---|---|
| PushPlus | Token |
| 钉钉群机器人 | Webhook、签名 |
| 飞书群机器人 | Webhook、签名 |
| 企业微信应用 | Corp ID、Agent ID、Secret |
| 企业微信群机器人 | Webhook |
| 电子邮箱 | SMTP SSL/STARTTLS |
| 短信直发 | 目标号码、仅断网时发送 |
| **Bark** | 服务地址、Device Key（**新增**） |
| **Gotify** | 服务地址、Token（**新增**） |

附加能力：五套转发模板、SIM 自定义名称、转发规则、失败重试、测试发送、免责声明。

---

## 安装与升级

| 项目 | 值 |
|---|---|
| 最低系统 | Android 8.0（API 26） |
| 编译 SDK | Android 16（API 36） |
| 正式包名 | `com.helyu.smsforwarder` |
| 版本 | **1.0.6** |
| versionCode | **1013** |

可从 **1.0.5（1012）** 及此前同签名正式版直接覆盖升级。Debug 包（`.debug` 后缀、不同签名）需先卸载再装正式版。

---

## 使用提醒

1. 确认仍为系统默认短信应用；进入 **设置 → 系统 → 后台运行与系统兼容** 检查三项短信状态。
2. 批量发送：默认 SIM / 间隔在折叠的「发送设置」中，用时点开即可。
3. 转发规则、远程转发默认关闭，启用前请阅读页内说明并测试。
4. 远程发送、批量发送均可能产生运营商资费。

---

## 隐私与安全

启用转发或远程功能后，短信内容会发往用户配置的第三方；请勿使用不可信接收端。Issue / 日志中勿提交 Token、Webhook、Secret、密码、签名文件或真实短信内容。

---

## 构建验证

- Android Lint、Debug/Release 编译、R8、APK 签名校验、包名版本核对、Fossify 假版本 DEX 扫描。

**验收结果（构建后填写）：**

| 项目 | 值 |
|---|---|
| 包名 | `com.helyu.smsforwarder` |
| 版本 | 1.0.6 |
| versionCode | 1013 |
| 大小 | （构建后填写） |
| APK SHA-256 | （构建后填写） |

正式签名 SHA-256：

```text
227d5ed7b74462e37d6e88c424eb0aad58a33acb1b6e193f2e2d08a650107c96
```

**Full Changelog:** https://github.com/2756826865/android-sms-forwarder/compare/v1.0.5...v1.0.6
