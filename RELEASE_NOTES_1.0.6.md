# SMS Forwarder 1.0.6

公开发布版本。基于 Fossify Messages 二次开发，可设置为 Android 默认短信应用，面向 Android 8.0–16 构建。

主要适配华为 EMUI/HarmonyOS、荣耀 MagicOS、小米 MIUI/HyperOS、vivo/iQOO，同时兼容 OPPO、一加、realme、三星和 Pixel 等主流 Android 设备。

## 本次更新

### 转发规则（默认关闭）

- 新增「转发 → 转发规则」，可按关键词、正则、来源 SIM 卡过滤短信，并指定规则作用的转发渠道。
- 支持三种作用范围：仅扩展渠道、含短信直发、含远程短信指令。
- 多个关键词支持逗号、竖线 `|`、分号或换行分隔。
- 提供填写示例与规则测试，便于核对匹配结果。

### 远程转发功能（默认关闭）

- 新增「转发 → 远程转发功能」入口，整合短信远程指令与钉钉 Stream 远程指令。
- **短信远程指令**：授权号码白名单；命令格式 `/短信发送 [SIM1|SIM2|默认|系统默认] 手机号 内容`；频率限制与命令去重；命令短信不进入正常转发流程。
- **钉钉 Stream 远程指令**：通过 Client ID / Client Secret 建立长连接；群聊 @机器人 或私聊发送相同格式命令；可指定 SIM1 / SIM2 / 默认发送卡。
- **发送回执**：本地执行日志始终记录；可选将提交/成功/失败/送达推送到已配置的转发渠道（与正常短信转发独立）。

### 新增转发渠道

- **Bark**：支持服务地址、Device Key；内网 HTTP 可单独开启「允许 HTTP」。
- **Gotify**：支持服务地址、Application Token；同样支持「允许 HTTP」。
- Bark、Gotify 设置页支持发送测试，风格与其他渠道设置页统一。

### 批量发送页面优化

- 「批量发送」合并为同一页面：收件人、短信正文、发送 SIM 卡、发送间隔（立即 / 1–5 秒）一处配置。
- 入口：**设置 → 批量发送**，或首页绿色 **+ → 批量发送**。
- 页面标题移至顶部导航栏居中显示。
- **发送设置**（SIM 卡 + 发送间隔）改为可折叠，默认收起；摘要行显示当前 SIM 与间隔，点击展开修改。
- 发送设置移至联系人列表上方，为联系人选择留出更多空间。
- 移除设置页中独立的「批量发送短信延迟」项；定时发送仍在 **设置 → 定时发送**，与批量间隔互不影响。

### 设置页分组调整

- **功能**：短信转发、定时发送、批量发送、骚扰拦截、最近删除、显示首页底部导航。
- **系统**：短信同步管理、后台运行与系统兼容。
- **其他**：关于。

### 首页导航与界面

- 首页底部「短信 · 转发 · 设置」胶囊导航默认开启，可在 **设置 → 功能 → 显示首页底部导航** 关闭。
- 关闭后首页仅保留短信列表与绿色 **+** 按钮；**+** 位置不变，返回首页后立即生效。
- Bark、Gotify、转发规则、远程转发、钉钉远程等设置页统一为 MIUI 浅色风格（灰底、描边输入框、绿色测试 / 黑色保存按钮）。
- 修复部分页面下拉菜单、输入框白底白字导致文字不可读的问题。
- 转发模板新增 **Emoji** 与 **自定义占位符**（`{sender}` `{name}` `{body}` `{time}` `{sim}`）模式。

### 修复

- 修复转发模板「自定义」模式下正文重复输出（原文 + 模板结果各一行）的问题。
- 修复选择「自定义」模板时，取消编辑仍会切换模式、且难以再次编辑模板内容的交互问题。

### 文档与说明

- 重写「关于 → 常见问题」（18 条）与「已知问题」（15 条），同步当前功能路径与使用说明。
- 远程指令、转发规则、钉钉远程设置页补充命令格式说明与填写示例。

## 转发功能

支持以下转发目标：

- PushPlus
- 钉钉群机器人
- 飞书群机器人
- 企业微信应用消息
- 企业微信群机器人
- 电子邮箱
- 短信直发
- **Bark**（新增）
- **Gotify**（新增）

同时支持：

- 简洁、标准、详细、**Emoji**、**自定义** 五套转发模板。
- SIM1/SIM2 自定义名称和本机号码。
- **转发规则**（关键词 / 正则 / SIM / 渠道 / 作用范围，默认关闭）。
- 黑名单、白名单和关键词拦截。
- 网络恢复后的失败重试与补偿任务。
- 发送测试消息和发送状态记录。
- 首次启用短信转发前展示隐私与安全免责声明。

## 主要功能

- 短信收发、会话搜索、标记已读、会话置顶和删除。
- 最近删除中的短信恢复与彻底删除。
- SIM1/SIM2 双卡识别和发送卡选择。
- 联系人选择及手动输入服务号码。
- TXT/CSV 文件导入及同一页批量发送（含可折叠发送设置）。
- 短信转发、失败重试和网络恢复补发。
- 定时短信和批量发送间隔。
- **转发规则**、**短信远程指令**、**钉钉 Stream 远程指令**（均默认关闭）。
- 新短信系统通知。
- 后台运行与默认短信应用状态检查。
- 各品牌自启动、电池策略和通知设置快捷入口。
- 首页底部导航可选显示/隐藏。

## 安装与升级

- 最低系统要求：Android 8.0（API 26）
- 编译 SDK：Android 16（API 36）
- 正式包名：`com.helyu.smsforwarder`
- 版本号：**1.0.6**
- versionCode：**1013**

正式版继续使用原包名和原正式签名，可从 **1.0.5（1012）及此前正式版本**直接覆盖升级。

Debug 版包名带 `.debug` 后缀且签名不同。从测试版迁移到正式版时，需要先卸载 Debug 版。

请勿安装来源不明或被第三方重新签名的 APK，否则可能无法覆盖升级，也无法确认代码和凭据安全。

## 使用提醒

安装或升级后建议确认：

1. 本应用仍为系统默认短信应用（部分华为、vivo 系统在重启或升级后可能换回系统「信息」应用）。
2. **设置 → 系统 → 后台运行与系统兼容** 中默认短信角色、底层路由、WRITE_SMS 均正常。
3. 批量发送时，默认 SIM 与间隔在「发送设置」折叠栏中；需要修改时点击展开。
4. 若使用转发规则，请先在「转发 → 转发规则」中理解作用范围，并用规则测试验证。
5. 若启用远程转发功能，请配置授权白名单或钉钉凭证，并知悉远程发送会产生运营商资费。

仍需允许：短信、通知、自启动、后台活动，并将电池策略设为「不限制」。

## 隐私与安全

- 启用短信转发后，短信内容会发送到用户主动配置的第三方服务（含 Bark、Gotify 等）。
- 启用远程转发功能后，授权号码或钉钉机器人可触发本机发送短信。
- 启用转发规则后，仅匹配规则的短信会转发到指定渠道；配置不当可能导致漏转或误转。
- 短信可能包含验证码、金融通知和个人信息，请勿配置不受信任的接收端。

不要将以下内容提交到 Issue、日志或公开聊天：

- Token、Webhook、Client Secret
- 企业微信 Secret、钉钉密钥
- 邮箱密码或授权码
- JKS 签名文件、签名密码
- 真实短信内容、验证码
- 包含个人号码的诊断信息

## 构建验证

本版本已完成：

- Android Lint 检查
- Core Debug 编译
- Core Release 编译
- R8 代码优化和资源裁剪
- APK ZIP 完整性检查
- APK Signature Scheme v2 验证
- 正式签名证书摘要核对
- 包名与版本号核对
- Fossify 假版本警告 DEX 扫描

**验收结果（构建后填写）：**

| 项目 | 值 |
|---|---|
| 包名 | `com.helyu.smsforwarder` |
| 版本 | 1.0.6 |
| versionCode | 1013 |
| 大小 | （构建后填写） |
| APK SHA-256 | （构建后填写） |

正式签名证书 SHA-256：

```text
227d5ed7b74462e37d6e88c424eb0aad58a33acb1b6e193f2e2d08a650107c96
```

不同厂商的默认短信路由、息屏后台行为、双卡识别、MMS、钉钉 Stream 长连接保活和邮件服务策略仍需持续收集真机反馈。

**Full Changelog:** https://github.com/2756826865/android-sms-forwarder/compare/v1.0.5...v1.0.6
